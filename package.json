{
  "name": "bookcheck-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "pnpm install && pnpm run agent:sync && pnpm run setup:check",
    "setup:check": "node --version && pnpm --version && uv --version && (podman --version || docker --version) && (aws --version || echo 'AWS CLI is not installed; see README.md 3-1')",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "lint": "oxlint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "check": "pnpm run fmt:check && pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run infra:check",
    "dc:up": "if command -v podman >/dev/null 2>&1; then podman compose -f compose.podman.yml up -d; else docker compose -f compose.yml up -d; fi",
    "dc:down": "if command -v podman >/dev/null 2>&1; then podman compose -f compose.podman.yml down; else docker compose -f compose.yml down; fi",
    "dc:ps": "if command -v podman >/dev/null 2>&1; then podman compose -f compose.podman.yml ps; else docker compose -f compose.yml ps; fi",
    "dc:logs": "if command -v podman >/dev/null 2>&1; then podman compose -f compose.podman.yml logs -f; else docker compose -f compose.yml logs -f; fi",
    "agent:sync": "cd agent/app/BookChecker && uv sync",
    "agent:local": "cd agent/app/BookChecker && uv run --env-file .env main.py",
    "agent:validate": "cd agent && INIT_CWD=\"$PWD\" agentcore validate",
    "agent:dev": "cd agent && INIT_CWD=\"$PWD\" agentcore dev",
    "agent:deploy": "cd agent && INIT_CWD=\"$PWD\" agentcore deploy",
    "agent:status": "cd agent && INIT_CWD=\"$PWD\" agentcore status",
    "infra:test": "pnpm --filter bookcheck-agent-infra run test",
    "infra:typecheck": "pnpm --filter bookcheck-agent-infra run typecheck",
    "infra:synth:dev": "pnpm --filter bookcheck-agent-infra run synth:dev",
    "infra:check": "pnpm run infra:typecheck && pnpm run infra:test",
    "seed:local": "node --env-file=.env.local scripts/seed-local.mjs"
  },
  "dependencies": {
    "@aws-amplify/ui-react": "6.15.5",
    "@aws-sdk/client-bedrock-agentcore": "3.1116.0",
    "@aws-sdk/client-dynamodb": "3.1116.0",
    "@aws-sdk/lib-dynamodb": "3.1116.0",
    "aws-amplify": "6.20.0",
    "aws-jwt-verify": "5.2.1",
    "next": "16.3.2",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "react-markdown": "10.1.0",
    "remark-gfm": "4.0.1"
  },
  "devDependencies": {
    "@aws-amplify/backend": "1.24.0",
    "@aws-amplify/backend-cli": "1.9.0",
    "@aws/agentcore": "0.27.1",
    "@types/node": "26.2.0",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@vitest/coverage-v8": "4.1.11",
    "aws-cdk": "2.1138.0",
    "aws-cdk-lib": "2.266.0",
    "aws-sdk-client-mock": "4.1.0",
    "constructs": "10.8.1",
    "esbuild": "0.25.10",
    "oxfmt": "0.64.0",
    "oxlint": "1.79.0",
    "tsx": "4.23.12",
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "packageManager": "pnpm@11.22.0"
}
